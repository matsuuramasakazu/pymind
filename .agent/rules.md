# プロジェクトルール

- Pythonスクリプトを実行する際は、必ず `.\venv\Scripts\activate` を使用して仮想環境を有効化すること。
- 直接コマンドを実行する場合は、`.\venv\Scripts\python.exe` を使用してもよい。
- 新しい依存関係を追加した場合は `pip freeze > requirements.txt` を更新すること。
- 実施計画（implementation_plan.md）やタスクリスト（task.md）を含むすべての計画ドキュメントは、常に日本語で出力してください。